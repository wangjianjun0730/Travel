<canvas class='container' canvas-id='background-canvas'>
  <cover-view class='shade-container' animation="{{showAnimation}}">
    <!-- <cover-view class='list-container'> -->
      <cover-view 
      listArr="{{['A','B','C']}}" 
      wx:key="{{index}}" 
      wx:for="{{['A','B','C']}}" 
      class='list-view' 
      animation="{{listAnimation}}">{{item}}
    </cover-view>
    <!-- </cover-view> -->
    
  </cover-view>
</canvas>

